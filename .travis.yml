language: node_js
sudo: true
dist: trusty
cache:
  directories:
  - "node_modules"
branches:
  only:
  - master
jobs:
  include:
    - stage: deploy
      script: skip
      deploy:
        provider: heroku
        api_key: $HEROKU_AUTH_TOKEN
